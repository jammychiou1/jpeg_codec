#include "decoder.h"

#include <iostream>
using std::cerr;

void process_scan(decoder_state_t& dcd) {
  cerr << "now processing...\n";
  // du_layout_t& du_layout = dcd.scan.du_layout;
  // int now_mcu = 0;
  // for (int i = 0; i < du_layout.y_mcu; i++) {
  //   for (int j = 0; j < du_layout.x_mcu; j++) {
  //     for (int k = 0; k < du_layout.n_du_per_mcu; k++) {

  //     }
  //     now_mcu++;
  //   }
  // }
}
